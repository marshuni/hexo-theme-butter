extends includes/document/layout.pug

block content
  - const noCardLayout = ['404'].includes(page.type) ? 'nc' : ''
  - var commentsJsLoad = false

  mixin commentLoad
    if page.comments !== false && theme.comments.use
      - commentsJsLoad = true
      !=partial('includes/integrations/comments/index', {}, {cache: true})

  #page(class=noCardLayout)
    if top_img === false && page.title
      .page-title= page.title

    case page.type
      when 'tags'
        include includes/sections/page/tags.pug
        +commentLoad
      when 'link'
        include includes/sections/page/friend-links.pug
        +commentLoad
      when 'categories'
        include includes/sections/page/categories.pug
        +commentLoad
      when '404'
        include includes/sections/page/404.pug
      default
        include includes/sections/page/default-page.pug
        +commentLoad
